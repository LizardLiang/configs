# Global opencode instructions

<!-- BEGIN kratos-sync -->
<!-- generated by scripts/sync-kratos.py from kratos 2.98.1 -- edits are overwritten -->

## Kratos Auto-Activation

When the user mentions **"Kratos"**, or addresses a god-agent by name (Athena, Ares, Metis, Apollo, Artemis, Hermes, Hephaestus, Daedalus, Clio, Mimir, Hades, Odysseus, Prometheus, Themis, Nemesis, Cassandra, Hera, Ananke, Iris),
or says "continue"/"next stage" during an active pipeline:

1. Read `C:/Users/shotu/.config/opencode/skills/kratos-auto/SKILL.md` first.
2. Follow its routing table to the matching `/kratos-*` command file under
   `C:/Users/shotu/.config/opencode/commands/`, and follow that file's instructions exactly.

Do not answer a Kratos-addressed request directly and do not improvise the
pipeline -- the command files own all routing, gating, and agent-spawn detail.
Spawn gods with the task tool using their opencode names (`kratos-ares`,
`kratos-athena`, ...), never `kratos:<name>`.

Kratos resources live in `C:/Users/shotu/.config/opencode/kratos` (pipeline/, modes/, rules/, templates/,
references/). Re-sync after a plugin upgrade with `python scripts/sync-kratos.py`.
<!-- END kratos-sync -->
